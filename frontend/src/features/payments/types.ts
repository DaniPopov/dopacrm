/**
 * Payments types — re-exported from the generated schema aliases.
 */
export type {
  Payment,
  PaymentMethod,
  PlanRevenueRow,
  RangeRevenue,
  RecordPaymentRequest,
  RefundPaymentRequest,
  RevenueSummary,
} from "@/lib/api-types"
