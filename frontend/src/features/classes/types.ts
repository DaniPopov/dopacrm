/**
 * Gym class types — re-exported from the OpenAPI-generated schema.
 *
 * Naming: the domain entity is `GymClass` rather than `Class` to avoid
 * any confusion with a JavaScript class. Same reason as the backend.
 */
export type {
  GymClass,
  CreateGymClassRequest,
  UpdateGymClassRequest,
} from "@/lib/api-types"
